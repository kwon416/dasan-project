:root {
  --page-bg: #f2f4f6;
  --surface: #ffffff;
  --text: #1a1d1f;
  --subtext: #5b6676;
  --muted: #8b95a1;
  --krds-gray-10: #f8fafc;
  --accent: #1a9332;
  --accent-2: #ffc700;
  --accent-3: #003675;
  --primary: #256ef4;
  --line: #e2e8f0;
  --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
  --shell-max: 1440px;
  --shell-pad: 24px;
  --control-radius: 8px;
  --radius: 16px;
  --up-text: #cc4a00;
  --down-text: #1d4ed8;
  --new-text: #7a32a8;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  color: var(--text);
  letter-spacing: -0.02em;
}

/* page */
.analysis-page {
  display: flex;
  flex-direction: column;
  background: linear-gradient(150deg, #fafdff 0%, #eef5ff 56%, #ffffff 100%);
}

/* nav-bar */
.nav-bar {
  width: 100%;
  border-bottom: 1px solid var(--line);
  background: rgba(255, 255, 255, 0.5);
}
.nav-inner {
  width: min(100%, var(--shell-max));
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 16px 32px;
  margin: 0 auto;
}
.identity {
  display: flex;
  gap: 12px;
}
.brand-title {
  font-size: 1.9rem;
  font-weight: 700;
}
.nav-logo img {
  width: 84px;
}
.home-btn {
  font-size: 1.3rem;
}
.home-btn a {
  display: flex;
  align-items: center;
  gap: 4px;
  opacity: 0.6;
}
.home-btn a::before {
  display: inline-block;
  content: "";
  width: 16px;
  height: 16px;
  background: url("assets/index/ico_bread_home.svg") no-repeat center / contain;
}

/* header */
.panel-shell {
  border: 1px solid var(--line);
  border-radius: var(--radius);
  background: var(--surface);
  box-shadow: var(--shadow);
  padding: 24px;
}
.analysis-header {
  position: relative;
  width: min(100%, var(--shell-max));
  display: flex;
  flex-direction: column;
  gap: 10px;
  border: none;
  border-radius: 0;
  background: transparent;
  box-shadow: unset;
  padding: 28px 32px 24px;
  margin: 0 auto;
  overflow: hidden;
}

.header-eyebrow {
  margin: 0;
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 1.1rem;
  font-weight: 700;
  letter-spacing: 0.08em;
  color: var(--primary);
}
.header-eyebrow::before {
  content: "";
  width: 18px;
  height: 2px;
  border-radius: 2px;
  background: var(--primary);
}

.analysis-title {
  margin: 10px 0 0;
  font-size: 2.4rem;
  line-height: 1.2;
}

.analysis-subtitle {
  margin: 8px 0 0;
  color: var(--subtext);
  font-size: 1.4rem;
}

.batch-updated {
  margin: 0;
  color: var(--muted);
  font-size: 1.25rem;
  font-weight: 600;
}

.analysis-grid {
  width: min(100%, var(--shell-max));
  display: grid;
  grid-template-columns: repeat(12, minmax(0, 1fr));
  align-items: stretch;
  gap: 24px;
  padding: var(--shell-pad) var(--shell-pad) 48px;
  margin: 0 auto;
}

.analysis-grid section {
  animation: rise 0.6s ease both;
}

.control-panel,
.trend-section {
  grid-column: 1 / -1;
}

.control-panel {
  animation-delay: 0.06s;
}

.trend-section {
  position: relative;
  z-index: 1;
}

.section-title {
  margin: 0;
  font-size: 1.9rem;
}

.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border: 0;
}

.control-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 14px;
  gap: 12px;
}

.analysis-context {
  margin: 6px 0 0;
  color: var(--subtext);
  font-size: 1.3rem;
}

.control-search-row {
  display: grid;
  grid-template-columns: 150px minmax(0, 1fr) auto auto;
  gap: 10px;
  align-items: center;
}

.control-select,
.control-input {
  width: 100%;
  height: 40px;
  border: 1px solid var(--line);
  border-radius: var(--control-radius);
  box-shadow: none;
  font-size: 1.3rem;
}

.control-select {
  padding: 0 36px 0 12px;
}

.control-input {
  padding: 0 14px;
}

.control-select:focus-visible,
.control-input:focus-visible,
.control-btn:focus-visible {
  outline: 2px solid rgba(49, 130, 246, 0.45);
  outline-offset: 1px;
}

.action-buttons {
  display: flex;
  align-items: center;
  gap: 8px;
}
.action-buttons .krds-btn {
  height: 40px;
  margin: 0;
  border-radius: var(--control-radius);
  font-size: 1.4rem;
}
.reset-btn {
  width: 100px;
  min-width: 100px;
  flex: 0 0 100px;
  border: 1px solid #cbd5e1 !important;
  background: #ffffff;
  color: #334155;
}
.reset-btn:hover {
  background: #f8fafc;
  border-color: #b7c3d3;
}
.search-btn {
  width: 120px;
  min-width: 120px;
  flex: 0 0 120px;
  background: var(--accent-3);
  border-color: var(--accent-3);
  color: #ffffff;
}
.search-btn:hover {
  background: #00468a;
  border-color: #00468a;
}
.district-field .krds-form-select:focus,
.search-input-field .krds-input:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(30, 77, 183, 0.12);
}

.search-row-bottom {
  display: flex;
  align-items: center;
  justify-content: flex-start;
  gap: 16px;
  margin-top: 20px;
}

.period-presets {
  flex: 0 0 auto;
}

.preset-group {
  display: inline-flex;
  align-items: center;
  height: 40px;
  overflow: hidden;
  border: 1px solid var(--line);
  border-radius: var(--control-radius);
  background: #ffffff;
  flex-wrap: nowrap;
  gap: 0;
}

.preset-btn {
  height: 40px;
  min-width: 76px;
  border: 0;
  border-right: 1px solid var(--line);
  border-radius: 0;
  background: #ffffff;
  color: #364152;
  padding: 0 16px;
  font-size: 1.4rem;
  font-weight: 600;
  cursor: pointer;
  transition: background-color 0.18s ease, border-color 0.18s ease, box-shadow 0.18s ease;
}

.preset-btn:hover {
  border-color: #aeb9c7;
  background: #f4f7fc;
}

.preset-btn:last-child {
  border-right: 0;
}

.preset-btn.is-active {
  background: var(--primary);
  color: #ffffff;
}

.date-range-field {
  height: 40px;
  border: 1px solid var(--line);
  border-radius: var(--control-radius);
  background: #ffffff;
  display: grid;
  grid-template-columns: minmax(0, 1fr) 20px minmax(0, 1fr);
  align-items: center;
  overflow: hidden;
}

.date-input.krds-input {
  height: 40px;
  border: 0;
  border-radius: 0;
  box-shadow: none;
  padding: 0 10px;
  font-size: 1.3rem;
}

.date-input.krds-input:focus {
  box-shadow: none;
  outline: none;
}

.range-separator {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  height: 40px;
  font-weight: 700;
  color: #4b5563;
}

.date-range-field:focus-within {
  border-color: var(--primary);
  box-shadow: 0 0 0 4px rgba(30, 77, 183, 0.12);
}

.control-note {
    margin: 12px 0 0;
    font-size: 1.24rem;
    color: #4f5f73;
}

.kpi-grid {
  margin-top: 20px;
  display: grid;
  grid-template-columns: repeat(12, minmax(0, 1fr));
  gap: 24px;
}

.kpi-card {
  position: relative;
  grid-column: span 3;
  padding: 20px;
  border-radius: 16px;
  background: #ffffff;
  border: 1px solid #e5e7eb;
  box-shadow: 0 2px 10px rgba(15, 23, 42, 0.05);
  display: flex;
  flex-direction: column;
  min-height: 212px;
}

.kpi-head {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  align-items: flex-start;
  gap: 6px;
}

.kpi-label {
  margin: 0;
  color: #6b7280;
  font-size: 13px;
  line-height: 1.4;
  font-weight: 600;
}

.kpi-period {
  margin: 0;
  color: #94a3b8;
  font-size: 11px;
  font-weight: 700;
  line-height: 1.2;
  letter-spacing: -0.01em;
  white-space: nowrap;
  text-align: right;
}

.kpi-main-row {
  margin-top: 14px;
  display: flex;
  align-items: baseline;
  gap: 8px;
}

.kpi-main {
  margin: 0;
  color: var(--text);
  font-size: 4rem;
  line-height: 1.4;
  font-weight: 700;
  letter-spacing: -0.03em;
}

.kpi-secondary {
  margin: 0;
  color: var(--subtext);
  font-size: 1.4rem;
  line-height: 1.2;
  font-weight: 600;
}

.kpi-bottom-row {
  margin-top: auto;
  padding: 11px 12px;
  border-top: 1px solid #f1f5f9;
  background: #f8fafc;
  border-radius: 10px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  gap: 8px;
}

.kpi-bottom-label {
  color: #6b7280;
  font-size: 12px;
  font-weight: 600;
}

.kpi-footer {
  margin: 0;
  color: #111827;
  font-size: 14px;
  line-height: 1.2;
  font-weight: 700;
  text-align: right;
  white-space: nowrap;
}

.kpi-footer.is-up,
.kpi-footer.is-new {
  color: #111827;
}

.kpi-footer.is-down {
  color: #111827;
}

.kpi-footer.is-flat {
  color: #111827;
}

.floating-tooltip {
  position: absolute;
  top: calc(100% + 10px);
  right: 0;
  min-width: 280px;
  max-width: 420px;
  border-radius: 8px;
  border: 1px solid #c8d5ea;
  background: #0f172a;
  color: #ffffff;
  font-size: 1.2rem;
  padding: 10px 12px;
  line-height: 1.4;
  white-space: normal;
  box-shadow: 0 8px 24px rgba(15, 23, 42, 0.34);
  z-index: 8;
}

.tooltip-title {
  margin: 0;
  font-size: 1.24rem;
  font-weight: 700;
}

.tooltip-list {
  margin: 8px 0 0;
  padding: 0;
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 6px;
  font-size: 1.15rem;
  line-height: 1.5;
}

.tooltip-copy {
  margin: 8px 0 0;
  font-size: 1.15rem;
  line-height: 1.58;
}

.is-hidden {
  display: none !important;
}

.trend-head {
  display: flex;
  align-items: flex-end;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}

.trend-summary {
  margin: 6px 0 0;
  color: var(--subtext);
  font-size: 1.3rem;
}

.trend-legend {
  margin: 0;
  display: inline-flex;
  align-items: center;
  gap: 10px;
  color: var(--subtext);
  font-size: 1.24rem;
  font-weight: 600;
}

.legend-current,
.legend-reference {
  display: inline-block;
  width: 22px;
  height: 4px;
  border-radius: 999px;
}

.legend-current {
  background: var(--primary);
}

.legend-reference {
  background: var(--line);
}

.comparison-chart-wrap {
  position: relative;
  border-radius: 16px;
  background: #f8fafc;
  box-shadow: inset 0 0 0 1px #edf2f8;
  margin-top: 16px;
  padding: 16px;
  overflow: hidden;
}

.comparison-chart-svg {
  width: 100%;
  display: block;
}

.chart-grid-line {
  stroke: #e5edf6;
  stroke-width: 1;
}

.chart-axis {
  fill: #8b95a1;
  font-size: 1.14rem;
}

.series-current {
  fill: none;
  stroke: var(--primary);
  stroke-width: 3.5;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.series-reference {
  fill: none;
  stroke: var(--line);
  stroke-width: 3;
  stroke-dasharray: 8 7;
  stroke-linecap: round;
  stroke-linejoin: round;
}

.point-current {
  fill: #ffffff;
  stroke: var(--primary);
  stroke-width: 2.2;
}

.point-reference {
  fill: #ffffff;
  stroke: #b8c0ca;
  stroke-width: 2;
}

.hover-band {
  fill: rgba(0, 0, 0, 0);
  cursor: pointer;
}

.chart-tooltip {
  position: absolute;
  min-width: 240px;
  max-width: 320px;
  border-radius: 14px;
  background: rgba(25, 31, 40, 0.9);
  color: #ffffff;
  padding: 12px;
  box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3);
  pointer-events: none;
  z-index: 7;
}

.chart-tooltip-title {
  margin: 0;
  font-size: 1.2rem;
  font-weight: 700;
}

.chart-tooltip-line {
  margin: 6px 0 0;
  font-size: 1.13rem;
  line-height: 1.45;
}

.chart-tooltip-note {
  margin: 8px 0 0;
  color: #c3d5ef;
  font-size: 1.08rem;
  line-height: 1.4;
}

.guide-row {
  position: relative;
  width: 100%;
  margin-top: 16px;
  display: inline-flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
}

.download-button {
    border: 0;
    border-radius: 999px;
    height: 34px;
    padding: 0 14px;
    font-size: 1.2rem;
    font-weight: 700;
    background: var(--accent-3);
    color: #fff;
    cursor: pointer;
}

.guide-button {
  border: 0;
  border-radius: 999px;
  height: 34px;
  padding: 0 14px;
  font-size: 1.2rem;
  font-weight: 700;
  background: #e8f1ff;
  color: var(--accent-3);
  cursor: pointer;
}

.guide-tooltip {
  top: calc(100% + 8px);
  right: 0;
  right: auto;
  width: min(560px, 92vw);
}

.region-map-section,
.district-section,
.activity-section,
.season-section {
  position: relative;
  padding: 24px;
}

.region-map-section {
  grid-column: span 7;
  animation-delay: 0.16s;
  display: flex;
  flex-direction: column;
  height: 850px;
}

.district-section {
  grid-column: span 5;
  animation-delay: 0.2s;
  display: flex;
  flex-direction: column;
  height: 850px;
}

.activity-section {
  grid-column: span 7;
  animation-delay: 0.24s;
}

.season-section {
  grid-column: span 5;
  animation-delay: 0.28s;
}

.section-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 14px;
  flex-wrap: wrap;
}

.section-head.compact {
  margin-bottom: 14px;
}

.section-copy {
  margin: 6px 0 0;
  color: var(--subtext);
  font-size: 1.3rem;
}

.map-body {
  display: flex;
  flex-direction: column;
  gap: 12px;
  min-height: 0;
  flex: 1;
}

.map-svg-wrap {
    width: 100%;
    min-height: 360px;
    background: var(--krds-gray-10);
    border-radius: 8px;
    padding: 16px;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    flex: 1;
}

.map-svg-wrap.is-error {
  background: #f8fafc;
  border-style: solid;
}

.map-svg {
  width: 100%;
  height: 100%;
  min-height: 0;
  max-height: none;
  display: block;
}

.district-path {
  fill: #dceeea;
  stroke: #f7f4ef !important;
  stroke-width: 1.8;
  stroke-linejoin: round;
  stroke-linecap: round;
  cursor: default;
  transition: fill 0.2s ease, filter 0.2s ease, stroke 0.2s ease;
  vector-effect: non-scaling-stroke;
}

.district-path[data-level="1"] { fill: #e8f0ff; }
.district-path[data-level="2"] { fill: #c7dbff; }
.district-path[data-level="3"] { fill: #9ec0ff; }
.district-path[data-level="4"] { fill: #6f95e1; }
.district-path[data-level="5"] { fill: #4e80da; }

.district-path:hover {
  filter: brightness(0.95);
}

.district-path.is-hovered {
  stroke: var(--accent-2) !important;
  fill: var(--primary) !important;
  stroke-width: 1.8;
  filter: drop-shadow(0 0 2px rgba(37, 110, 244, 0.45));
}

.map-svg path[fill="none"] {
  stroke: #d1d6db !important;
  stroke-width: 2.5;
}

.map-svg text {
  font-family: "Pretendard", "Noto Sans KR", sans-serif;
  font-size: clamp(28px, 3.2vw, 52px);
  fill: #21423f;
  pointer-events: none;
  opacity: 0.25;
  font-weight: 700;
  paint-order: stroke;
  stroke: rgba(247, 244, 239, 0.95);
  stroke-width: 6px;
}

.map-svg text.is-active,
.map-svg text.is-selected {
  opacity: 1;
  fill: var(--text);
  stroke: rgba(255, 255, 255, 0.95);
}

.map-legend {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
  font-size: 1.2rem;
  font-weight: 600;
  color: var(--muted);
}

.map-legend-bottom {
  margin-top: 8px;
  background: rgba(255, 255, 255, 0.96);
  padding: 4px 8px;
  border-radius: 6px;
}

.legend-title {
  font-weight: 700;
}

.legend-bar {
  display: flex;
  align-items: center;
  gap: 8px;
}

.legend-gradient {
  display: inline-block;
  width: 180px;
  height: 10px;
  border-radius: 999px;
  background: linear-gradient(90deg, #e8f0ff, var(--primary));
}

.legend-min,
.legend-max {
  font-variant-numeric: tabular-nums;
  color: var(--text);
  font-weight: 700;
  font-size: 1.6rem;
}

.district-ranking-bars {
  margin: 14px 0 0;
  padding: 0 6px 0 0;
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 12px;
  flex: 1;
  min-height: 0;
  overflow-y: auto;
}

.district-ranking-bars::-webkit-scrollbar {
  width: 6px;
}

.district-ranking-bars::-webkit-scrollbar-track {
  background: transparent;
}

.district-ranking-bars::-webkit-scrollbar-thumb {
  background: #cbd5e1;
  border-radius: 999px;
}

.district-item {
  border-radius: 16px;
  padding: 14px 14px 12px;
  background: #f9fbff;
  box-shadow: inset 0 0 0 1px #edf2f8;
  transition: box-shadow 0.16s ease, transform 0.16s ease, background 0.16s ease;
}

.district-item.rank-top1 {
  background: linear-gradient(130deg, #0d5dc2 0%, #3182f6 100%);
  box-shadow:
    inset 0 0 0 1px rgba(255, 255, 255, 0.14),
    0 8px 22px rgba(49, 130, 246, 0.2);
}

.district-item.rank-top1 .district-rank {
  background: rgba(255, 255, 255, 0.24);
  color: #ffffff;
}

.district-item.rank-top1 .district-name,
.district-item.rank-top1 .district-percent,
.district-item.rank-top1 .district-count {
  color: #ffffff;
}

.district-item.rank-top1 .district-track {
  background: rgba(255, 255, 255, 0.22);
}

.district-item.rank-top1 .district-fill {
  background: linear-gradient(90deg, #d7ebff 0%, #ffffff 100%);
}

.district-item.rank-top2 {
  background: linear-gradient(130deg, #d8e7fb 0%, #d8e9ff 100%);
  box-shadow: inset 0 0 0 1px rgba(49, 130, 246, 0.22);
}

.district-item.rank-top3 {
  background: linear-gradient(130deg, #f5f9ff 0%, #ebf4ff 100%);
  box-shadow: inset 0 0 0 1px rgba(49, 130, 246, 0.15);
}

.district-item.is-row-hover {
  transform: translateY(-1px);
  box-shadow:
    inset 0 0 0 1px rgba(49, 130, 246, 0.44),
    0 8px 20px rgba(49, 130, 246, 0.16);
}

.district-item.is-highlight,
.district-item.is-selected {
  background: linear-gradient(145deg, #fff7d6 0%, #fff1b8 100%);
  box-shadow:
    inset 0 0 0 1px rgba(250, 204, 21, 0.68),
    0 10px 20px rgba(234, 179, 8, 0.18);
}

.district-item.is-selected .district-rank {
  background: #facc15;
  color: #7a5300;
}

.district-item.is-selected .district-name,
.district-item.is-selected .district-percent,
.district-item.is-selected .district-count {
  color: #4c3800;
}

.district-item.is-selected .district-track {
  background: rgba(250, 204, 21, 0.32);
}

.district-item.is-selected .district-fill {
  background: linear-gradient(90deg, #facc15 0%, #f59e0b 100%);
}

.district-head {
  display: grid;
  grid-template-columns: auto 1fr auto;
  align-items: center;
  gap: 10px;
}

.district-rank {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 38px;
  height: 24px;
  border-radius: 999px;
  background: #eaf2ff;
  color: var(--accent-3);
  font-size: 1rem;
  font-weight: 700;
}

.district-name {
  color: var(--text);
  font-size: 1.3rem;
  font-weight: 700;
}

.district-percent {
  color: var(--subtext);
  font-size: 1.2rem;
  font-weight: 700;
}

.district-track {
  margin-top: 9px;
  height: 9px;
  border-radius: 999px;
  background: #e6e8ea;
  overflow: hidden;
}

.district-fill {
  display: block;
  height: 100%;
  border-radius: inherit;
  background: linear-gradient(90deg, var(--accent-3), var(--primary));
}



.district-count {
  margin: 7px 0 0;
  text-align: right;
  color: var(--subtext);
  font-size: 1.2rem;
  font-weight: 600;
}

.district-empty {
  margin: 0;
  border-radius: 14px;
  padding: 14px;
  background: #f8fafc;
  color: var(--muted);
  font-size: 1.1rem;
}

.activity-overview {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 8px;
}

.insight-chip {
  display: inline-flex;
  align-items: center;
  height: 32px;
  border-radius: 999px;
  padding: 0 12px;
  background: #e8f1ff;
  color: var(--accent-3);
  font-size: 1.1rem;
  font-weight: 700;
  box-shadow: inset 0 0 0 1px rgba(49, 130, 246, 0.25);
}

.activity-peak-label {
  margin: 0;
  color: var(--subtext);
  font-size: 1.2rem;
  font-weight: 600;
  text-align: right;
}

.activity-grid-shell {
  position: relative;
  margin-top: 16px;
  border-radius: 18px;
  padding: 16px;
  background: linear-gradient(158deg, #fcfdff 0%, #f5faff 62%, #ffffff 100%);
  box-shadow: inset 0 0 0 1px #e8eef6;
}

.activity-grid-wrap {
  min-height: 260px;
}

.activity-table {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.activity-header,
.activity-row {
  display: grid;
  grid-template-columns: 30px repeat(24, minmax(0, 1fr));
  gap: 4px;
  align-items: center;
}

.activity-hour,
.activity-day {
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

.activity-hour {
  color: #92a0b2;
  font-size: 0.94rem;
  font-weight: 600;
}

.activity-day {
  color: var(--subtext);
  font-size: 1.08rem;
  font-weight: 700;
}

.activity-cell {
  appearance: none;
  border: 0;
  width: 100%;
  height: 16px;
  border-radius: 7px;
  background: var(--activity-color, #f2f4f6);
  box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.75);
  cursor: pointer;
  position: relative;
  transition: transform 0.15s ease, box-shadow 0.15s ease, filter 0.15s ease;
}

.activity-cell:hover,
.activity-cell:focus-visible {
  outline: none;
  transform: translateY(-1px);
  filter: saturate(1.05);
  box-shadow:
    inset 0 0 0 1px rgba(255, 255, 255, 0.82),
    0 5px 12px rgba(49, 130, 246, 0.24);
}

.activity-cell.is-peak {
  box-shadow:
    inset 0 0 0 1px rgba(255, 255, 255, 0.84),
    0 0 0 4px rgba(49, 130, 246, 0.16),
    0 8px 18px rgba(49, 130, 246, 0.25);
}

.activity-cell.is-peak::after {
  content: attr(data-peak);
  position: absolute;
  top: -22px;
  left: 50%;
  transform: translateX(-50%);
  height: 18px;
  padding: 0 7px;
  border-radius: 999px;
  background: var(--accent-3);
  color: #ffffff;
  font-size: 0.9rem;
  font-weight: 700;
  line-height: 18px;
  white-space: nowrap;
}

.activity-legend {
  margin-top: 8px;
  display: flex;
  justify-content: flex-end;
  align-items: center;
  gap: 8px;
  color: var(--muted);
  font-size: 1.03rem;
  font-weight: 600;
}

.activity-scale {
  display: inline-block;
  width: 152px;
  height: 8px;
  border-radius: 999px;
  background: linear-gradient(90deg, #f2f4f6 0%, #e8eef8 22%, #d5e5fb 42%, #b8d4fb 63%, #88b9fb 82%, #3182f6 100%);
}

.season-grid {
  margin-top: 14px;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.season-card {
  position: relative;
  overflow: hidden;
  border-radius: 20px;
  padding: 24px;
  background: #ffffff;
  box-shadow:
    var(--shadow),
    inset 0 0 0 1px rgba(226, 233, 242, 0.9);
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.season-card::before {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: 0.05;
}

.season-spring::before {
  background: linear-gradient(145deg, #6ed39b 0%, #f2fff8 100%);
}

.season-summer::before {
  background: linear-gradient(145deg, #8ec2ff 0%, #f0f8ff 100%);
}

.season-autumn::before {
  background: linear-gradient(145deg, #ffb982 0%, #fff8f2 100%);
}

.season-winter::before {
  background: linear-gradient(145deg, #b7d6ff 0%, #f4f8ff 100%);
}

.season-top-row {
  position: relative;
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12px;
}

.season-label-group {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  min-width: 0;
}

.season-icon {
  width: 24px;
  height: 24px;
  border-radius: 999px;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-size: 14px;
  line-height: 1;
  background: #eef2f6;
}

.season-spring .season-icon {
  background: rgba(110, 211, 155, 0.2);
}

.season-summer .season-icon {
  background: rgba(142, 194, 255, 0.2);
}

.season-autumn .season-icon {
  background: rgba(255, 185, 130, 0.22);
}

.season-winter .season-icon {
  background: rgba(183, 214, 255, 0.26);
}

.season-name {
  margin: 0;
  color: var(--subtext);
  font-size: 1.3rem;
  font-weight: 700;
}

.season-metric-group {
  display: inline-flex;
  flex-direction: column;
  align-items: flex-end;
  gap: 7px;
}

.season-badge {
  display: inline-flex;
  align-items: center;
  height: 22px;
  border-radius: 999px;
  padding: 0 9px;
  background: #e8f1ff;
  color: var(--accent-3);
  font-size: 0.95rem;
  font-weight: 700;
}

.season-percent {
  margin: 0;
  color: #152b45;
  font-size: 2.4rem;
  font-weight: 800;
  line-height: 1;
  letter-spacing: -0.02em;
}

.season-count {
  margin: 0;
  color: #7e8a99;
  font-size: 1.2rem;
  font-weight: 600;
}

.season-progress {
  position: relative;
  height: 8px;
  border-radius: 999px;
  background: #e6e8ea;
  overflow: visible;
}

.season-current-bar,
.season-reference-tick {
  position: absolute;
  top: 50%;
}

.season-current-bar {
  left: 0;
  height: 100%;
  transform: translateY(-50%) scaleX(0);
  border-radius: inherit;
  width: var(--current-width, 0%);
  background: linear-gradient(90deg, #86b5fb 0%, var(--primary) 100%);
  z-index: 2;
  transform-origin: left center;
  animation: seasonBarFill 0.66s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
}

.season-reference-tick {
  left: var(--reference-pos, 0%);
  width: 2px;
  height: 14px;
  transform: translate(-1px, -50%);
  border-radius: 999px;
  background: rgba(116, 126, 139, 0.85);
  box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.9);
  z-index: 3;
}

.season-yoy {
  margin: 0;
  font-size: 1.2rem;
  font-weight: 700;
}

.season-yoy.is-up {
  color: var(--up-text);
}

.season-yoy.is-down {
  color: var(--down-text);
}

.season-yoy.is-new {
  color: var(--new-text);
}

.season-yoy.is-flat {
  color: var(--subtext);
}

@keyframes seasonBarFill {
  from {
    transform: translateY(-50%) scaleX(0);
  }
  to {
    transform: translateY(-50%) scaleX(1);
  }
}

.skeleton-row {
  border-radius: 14px;
  padding: 14px;
  background: #ffffff;
  box-shadow: var(--shadow);
}

.skeleton-line {
  width: 100%;
  height: 12px;
  border-radius: 6px;
  background: linear-gradient(90deg, #edf3fa 25%, #dde8f5 37%, #edf3fa 63%);
  background-size: 400% 100%;
  animation: shimmer 1.2s ease infinite;
}

.skeleton-line + .skeleton-line {
  margin-top: 8px;
}

.skeleton-line.short {
  width: 38%;
}

.skeleton-line.medium {
  width: 62%;
}

@keyframes shimmer {
  0% {
    background-position: 100% 0;
  }
  100% {
    background-position: 0 0;
  }
}

@keyframes panelEnter {
  0% {
    opacity: 0;
    transform: translateY(12px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes rise {
  from {
    opacity: 0;
    transform: translateY(10px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@media (prefers-reduced-motion: reduce) {
  .panel-shell {
    opacity: 1;
    transform: none;
    animation: none;
  }

  .period-tab,
  .chart-tooltip,
  .hover-band {
    transition: none;
  }

  .skeleton-line {
    animation: none;
  }

  .season-current-bar {
    animation: none;
    transform: translateY(-50%) scaleX(1);
  }
}

@media (max-width: 1024px) {
  .analysis-context {
    text-align: left;
  }

  .control-search-row {
    grid-template-columns: 1fr minmax(0, 2fr) auto auto;
  }

  .search-row-bottom {
    flex-wrap: wrap;
    gap: 12px;
    margin-top: 16px;
  }

  .period-presets {
    flex: 0 0 auto;
  }

  .period-range {
    min-width: 0;
    flex: 1 1 auto;
  }

  .kpi-card {
    grid-column: span 6;
  }

  .region-map-section,
  .district-section,
  .activity-section,
  .season-section {
    grid-column: 1 / -1;
    height: auto;
  }

  .season-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .activity-overview {
    align-items: flex-start;
  }

  .activity-peak-label {
    text-align: left;
  }

  .map-svg-wrap {
    min-height: 280px;
  }
}

@media (max-width: 768px) {
  .control-head {
    flex-direction: column;
    align-items: flex-start;
    gap: 0;
  }

  .action-buttons {
    width: 100%;
    min-width: 0;
    flex: 0 0 auto;
  }

  .reset-btn, .search-btn {
    width: 100%;
    min-width: 0;
    flex: 1 1 0;
  }

    .search-row-bottom {
    flex-direction: column;
    align-items: stretch;
    gap: 10px;
    margin-top: 16px;
  }

  .preset-group {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    height: auto;
    width: 100%;
  }

  .preset-btn {
    min-width: 0;
    border-right: 1px solid var(--line);
    border-bottom: 1px solid var(--line);
  }

  .preset-btn:nth-child(2n) {
    border-right: 0;
  }

  .preset-btn:nth-last-child(-n + 2) {
    border-bottom: 0;
  }

  .period-range {
    width: 100%;
    min-width: 0;
    flex: 1 1 auto;
  }

  .date-range-field {
    grid-template-columns: minmax(0, 1fr) 16px minmax(0, 1fr);
  }

  .kpi-bottom-row {
    margin-top: 16px;
  }
  
  .kpi-grid {
    gap: 14px;
  }

  .control-search-row {
    grid-template-columns: 1fr;
  }

  .control-btn {
    width: 100%;
  }

  .control-bottom-row {
    flex-direction: column;
    align-items: stretch;
  }

  .period-tabs {
    width: 100%;
  }

  .period-tab {
    flex: 1 1 25%;
    min-width: 0;
  }

  .date-range-display {
    width: 100%;
    justify-content: space-between;
  }

  .custom-range-wrap {
    width: 100%;
    justify-content: space-between;
    height: auto;
    padding: 8px 10px;
  }

  .custom-date-input {
    min-width: 0;
    width: calc(50% - 10px);
  }

  .kpi-card {
    grid-column: span 12;
    min-height: 0;
  }

  .kpi-main {
    font-size: 36px;
  }

  .kpi-secondary {
    font-size: 13px;
  }

  .kpi-period {
    font-size: 10px;
  }

  .guide-tooltip {
    width: unset;
  }

  .comparison-chart-svg {
    height: 180px;
  }

  .chart-tooltip {
    max-width: 260px;
  }

  .section-copy {
    font-size: 1.13rem;
  }

  .map-svg-wrap {
    min-height: 220px;
    padding: 8px;
  }

  .map-svg text {
    font-size: clamp(18px, 3.3vw, 30px);
    stroke-width: 4.5px;
  }

  .legend-gradient {
    width: 120px;
  }

  .legend-min,
  .legend-max {
    font-size: 0.95rem;
  }

  .activity-grid-shell {
    padding: 12px;
  }

  .activity-header,
  .activity-row {
    grid-template-columns: 24px repeat(24, minmax(0, 1fr));
    gap: 3px;
  }

  .activity-day {
    font-size: 1rem;
  }

  .activity-cell {
    height: 13px;
    border-radius: 6px;
  }

  .activity-cell.is-peak::after {
    display: none;
  }

  .district-item {
    padding: 12px;
  }

  .season-grid {
    grid-template-columns: 1fr;
    gap: 14px;
  }
}
